"""
wyzer.desktop — Deterministic desktop subsystem (Phase 16).

Candidate resolution, click-and-type orchestration, overlay selection.
Separate from wyzer.tools.desktop (which houses individual ToolBase tools).
"""
